---
- bmg: |-
    suppliers.minus(suppliers)
  sqlite: |-
    SELECT
      `t1`.`sid`,
      `t1`.`name`,
      `t1`.`city`,
      `t1`.`status`
    FROM
      `suppliers` AS 't1'
    EXCEPT 
    SELECT
      `t1`.`sid`,
      `t1`.`name`,
      `t1`.`city`,
      `t1`.`status`
    FROM
      `suppliers` AS 't1'
- bmg: |-
    suppliers.minus(suppliers).minus(suppliers)
  sqlite: |-
    SELECT
      `t1`.`sid`,
      `t1`.`name`,
      `t1`.`city`,
      `t1`.`status`
    FROM
      `suppliers` AS 't1'
    EXCEPT
    SELECT
      `t1`.`sid`,
      `t1`.`name`,
      `t1`.`city`,
      `t1`.`status`
    FROM
      `suppliers` AS 't1'
    EXCEPT
    SELECT
      `t1`.`sid`,
      `t1`.`name`,
      `t1`.`city`,
      `t1`.`status`
    FROM
      `suppliers` AS 't1'
